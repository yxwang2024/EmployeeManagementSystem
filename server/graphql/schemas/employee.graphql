type Query {
  getEmployees: [Employee]
  getEmployee(employeeId: ID!): Employee
}

type Employee {
  id: ID!
  email: String!
  username: String!
  profile: Profile
  onboardingApplication: OnboardingApplication
}